import { createLazyBlock } from "./createLazyBlock";
import { getLazyCommand } from "./getLazyCommand";

export { createLazyBlock, getLazyCommand };
export default { createLazyBlock, getLazyCommand };
